# Get this from Hygraph Studio > Project Settings > API Access > Content API
HYGRAPH_ENDPOINT=https://api-<region>.hygraph.com/v2/<project-id>/master

# Optional: Permanent Auth Token for draft content
# Get this from Hygraph Studio > Project Settings > API Access > Permanent Auth Tokens
HYGRAPH_TOKEN=
